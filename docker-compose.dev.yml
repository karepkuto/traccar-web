services:
  traccar-web-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: traccar-web-dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
